{
  "builds": [
    {
      "src": "requirements.txt",
      "use": "@vercel/python",
      "config": {
        "buildCommand": "pip install rust && pip install -r requirements.txt"
      }
    }
  ]
}
